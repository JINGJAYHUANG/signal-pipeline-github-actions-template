# v0.1.0

Initial public release.

- deterministic synthetic provider;
- weighted threshold selector;
- duplicate suppression with TTL state;
- dry-run, console, file and HTTPS webhook modes;
- retry and acknowledgement semantics;
- isolated Git state branch;
- atomic artifacts and SHA-256 manifest;
- GitHub Actions CI, schedule and release workflows;
- Python 3.11–3.13 verification;
- public-safety and immutable-action audit.
